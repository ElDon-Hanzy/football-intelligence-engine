name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install UI v2 dependencies
        working-directory: frontend-v2
        run: npm install --no-audit --no-fund

      - name: Typecheck UI v2
        working-directory: frontend-v2
        run: npm run typecheck

      - name: Unit test UI v2
        working-directory: frontend-v2
        run: npm test

      - name: Build UI v2
        working-directory: frontend-v2
        run: npm run build

      - name: Install Chromium for UI v2 E2E
        working-directory: frontend-v2
        run: npx playwright install --with-deps chromium

      - name: E2E and accessibility smoke UI v2
        working-directory: frontend-v2
        run: npm run test:e2e

      - name: Bootstrap UI v2 lockfile into Git
        run: |
          if git status --porcelain frontend-v2/package-lock.json | grep -q .; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add frontend-v2/package-lock.json
            git commit -m "C0169 lock UI v2 dependencies"
            git push origin HEAD:main
          fi

      - name: Prepare Pages artifact
        run: |
          mkdir -p _site
          rsync -a ./ _site/ \
            --exclude '.git/' \
            --exclude '.github/' \
            --exclude 'frontend-v2/' \
            --exclude '_site/' \
            --exclude 'node_modules/'
          mkdir -p _site/v2
          cp -R frontend-v2/dist/. _site/v2/

      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

// C0175 deterministic screenshot hashes. Keys are <playwright-project>:<route>.
// Baselines are intentionally text-only so every visual change is reviewable in Git history.
export const visualBaselines: Record<string, string> = {};

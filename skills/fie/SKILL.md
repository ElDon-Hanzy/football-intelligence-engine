# Football Intelligence Engine Operating Skill

## Source-of-truth order
1. Live Supabase runtime/data/architecture registry.
2. `public.change_tracker_working` and C0213 governance.
3. Current GitHub source/migrations.
4. Canonical docs.
5. Historical handovers/conversation summaries.

Never overwrite live evidence with stale documentation. Resolve current run IDs and governance state from live runtime.

## Start-of-session checks
Read `PROJECT_STATE.md`, `DECISIONS_AND_HISTORY.md`, latest relevant closeout/active plan; inspect manager state and current prediction/optimizer/sequential lineage; inspect tracker; run C0213 architecture + behavioral status; verify current-GW inputs; inspect C0276/C0281 cycle/deadline/final/publication governance.

## Decision rules
Optimize the whole legal XV. Captaincy is separate. Always compare ROLL. Hits need robust edge after FT inventory, flexibility, timing and uncertainty. Sequential planning models bank, selling value, FT carry/accrual and future information. Bench value is expected autosub/resilience, not a flat percentage; Bench Boost uses full bench value. Price affects feasibility/timing, never xPts. Serious challengers remain until resolved. Options inside model error are `NO_MEANINGFUL_EDGE`. Unpromoted research cannot gain production effect.

## Noise-Control / anti-over-engineering
Never act from one model, recent score, isolated statistic or one-match tactical observation. Require multiple independent signals including a structural signal. If reasonable assumptions flip the recommendation or the edge is inside model error, do not act. Before adding a layer require a demonstrated decision failure, unique information, falsifiable contract, one clear consumer and decision value above complexity; otherwise consolidate/reject.

Monitoring is not a permanent parking state. Bounded research must have an expiry/adjudication point. At expiry: promote, retire/reject, or register a materially new bounded hypothesis. Do not keep collecting until significance appears and do not retune thresholds on the prospective outcome window.

## Canonical architecture
`FULL-POOL OPTIMIZER` → uncertainty → ensemble/equivalence → structural robustness → forward-management → OR/rank utility → red-team → C0240 adversarial benchmark → captaincy/named-challenger consistency → **C0248 sole canonical sequential selected-path authority** → C0277 chip opportunity-cost supporting gate → C0234/C0276 fail-closed final authorization → C0237/publication → external execution only if separately authorized.

C0276 is the bounded autonomy control plane, not an optimizer. Preserve exact lineage, bounded retries, event invalidation, deadline governance, historical immutability and no external transfer/chip execution.

## C0281 deadline convergence directive — Completed / Verified
Use `NORMAL → T4_BASELINE → T4_BASELINE_CONVERGED → T2_DELTA → CLOSED`.
- T-4 is the target for coherent baseline convergence.
- T-2 handles material deltas; do not wait until T-2 to build the chain.
- authoritative player-state changes remain immediately material;
- fixture-signature churn inside T-4 is conservatively debounced under the registered policy;
- respect empirical runtime reservation; do not launch new heavy work after reservation breach;
- freeze only a coherent checkpoint that still passes existing governance; otherwise fail closed;
- async RUNNING work must terminalize through governed HTTP/TTL reconciliation;
- reuse C0243/C0248 price timing: price cannot create a football transfer, only accelerate an already robust action when affordability is materially threatened.

## C0248 invariants
C0248 remains sole selected-path authority. Fresh candidate lineage is not self-promoting. All chip/terminal/price/FH controls used together bind to canonical planner lineage or fail closed. Free Hit uses a fresh one-GW optimizer candidate; Wildcard uses a fresh-squad sequential path. Wildcard and Free Hit retain banked FTs under 2026/27 rules.

## C0277 seasonal chip option-value directive — Completed / Verified
Use C0277 as supporting evidence, never a second transfer optimizer. Keep exact numerical decision horizon distinct from seasonal chip opportunity window; unknown future option value is not zero; structural future evidence can reserve but not authorize a chip; do not invent probabilities for unconfirmed BGW/DGW scenarios; shared future windows cannot be double-counted; best future chip week remains unresolved until evidence supports precision. Chip spend requires `PLAY_NOW` plus whole-squad reoptimization, xMins/role gates, opportunity cost, red-team/Noise-Control and final timing authorization.

## C0278 whole-engine reconciliation rule
Before a major architecture/model program after a long implementation chain, reconcile runtime/registry/tracker/GitHub/docs. Audit active crons and authority, preserve intentional bounded research, rerun C0213 governance and do not begin a new feature while a P0 source-of-truth contradiction remains unresolved.

## C0279/C0280 season-state / score-family / player-discovery directive
C0279 predictive integration remains promotion blocked / zero numeric production effect. C0280 promoted only the P9 calculation-faithful explanation/modal contract; P2-P8 predictive effects remain held pending prospective evidence.

Rules:
- never restore L20 as a model input;
- previous-season team-performance weight reaches zero from nine current-season matches;
- keep venue/opponent/tactical/personnel/availability/congestion modifiers outside the season blend;
- classify LOW/NORMAL/HIGH before script; shootout/demolition are HIGH subtypes;
- raw modal cell is diagnostic, not automatically the representative headline;
- player-upside research cannot bypass projection/captaincy/C0277/C0248/C0276 authority;
- explanation facts must be material, chronology-safe, directionally correct and traceable;
- research-only context stays separately labeled with zero numeric effect.

## C0285 adaptive-evidence / match-state repair — Planned, zero production effect
C0285 is a critical bounded forecasting-quality program registered after the GW5 forensic review. Canonical plan: `project-management/C0285_ADAPTIVE_EVIDENCE_MATCH_STATE_REPAIR_PLAN_20260921.md`.

Do not implement it as a blanket recent-form uplift. First falsify whether heavy shrinkage of chronology-safe current evidence is repeatedly harmful. Structural/current disagreement should be eligible to widen uncertainty rather than being silently normalized away. Candidate layers are adaptive evidence reconciliation, contradiction gate, process-based Team State, reliable weapon×vulnerability matchup interactions, and scoring-environment/scenario mixtures. Validate each increment chronologically and by ablation; promote the simplest robust improvement only. Exact-score matching and winner hit rate are not optimization targets; proper probabilistic scoring and calibration are primary. No C0285 numeric production effect until C0213 proof, bounded prospective evidence, downstream player/FPL recalibration and governed promotion. C0284 remains production control during the program. C0265 remains untouched.

## Transfer-window disposition
C0203/C0205/C0206 are Completed/Verified. C0207-C0211 are closed/superseded. Do not keep transfer-window projects alive merely because current-season minutes/roles/set pieces continue to update. New concrete transfer/newcomer defects require a new bounded issue.

## Bounded research expiry
C0197, C0224 and C0230 have GW6 adjudication boundaries. C0230 remains advisory/nonblocking and zero numeric effect. C0270 remains a prospective shadow anomaly watch.

## Product semantics
C0282 is Completed/Verified. V3 is the current consumer surface and V2 remains fallback until explicitly retired. Preserve Actual / Recommendation / Decision snapshot / Realized separation. Live player observations before fixture finality are not final realized values. `FINAL` never implies external execution.

## Protected states
C0265 predicted-XI/xMins behavior is deliberately unchanged; do not repair without explicit authorization. C0270 is shadow observation only. Do not change C0240 concurrency without separate validation/authorization. Never rewrite historical forecasts.
